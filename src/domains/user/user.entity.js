/*
    id: string
    phone: string
    firstName: string
    lastName: string
    nickname: string
    email: string(optional)
    birthday: date(optional)
    bio: string(optional)
    imageUrl: string(optional)
*/
module.exports = class User {};